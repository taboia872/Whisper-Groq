<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base app theme -->
    <style name="Theme.WhisperGroq" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent_container</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:windowBackground">@color/windowBackground</item>
        <item name="android:statusBarColor">@color/windowBackground</item>
    </style>

    <!-- With an action bar -->
    <style name="Theme.WhisperGroq.ActionBar" parent="Theme.Material3.DayNight">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent_container</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:windowBackground">@color/windowBackground</item>
    </style>

    <!-- Floating dialog style for RecognizeActivity -- use plain Material dialog -->
    <style name="Theme.WhisperFloating" parent="Theme.Material3.DayNight.Dialog">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="android:windowBackground">@color/surface</item>
    </style>

</resources>
